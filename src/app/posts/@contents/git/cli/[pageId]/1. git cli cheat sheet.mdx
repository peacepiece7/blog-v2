# Branch rename

`git branch -M <target-branch> <new-name>`

# Remove untracked files and directories

`git clean -fd`

# Merge no fast forward and edit commit message

`git merge <branch> --no-ff --edit`

# Remove cache

`git rm -r --cached .`
