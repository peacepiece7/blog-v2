# src/app/posts/@contents/example/tailwind/[pageId]/page.tsx

props의 interface를 선언해주면서 샌긴 타입에러, 함수 안에 타입을 작성해주면된다.

```js
export default function PagePage({ param }: { param : { slug : string }})
```
